<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/imageServiceIcon"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_alignParentTop="true"
        android:layout_marginTop="10dp"
        android:contentDescription="@string/service_icon"
        android:padding="10dp"
        android:scaleType="fitXY"
        android:src="@drawable/digital_gate" />

    <TextView
        android:id="@+id/textServiceName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:layout_toEndOf="@id/imageServiceIcon"
        android:layout_toRightOf="@id/imageServiceIcon"
        android:padding="10dp"
        android:textColor="@color/nmBlack"
        android:textSize="18sp" />

</RelativeLayout>